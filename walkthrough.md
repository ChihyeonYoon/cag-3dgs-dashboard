# Walkthrough: 관상동맥 3DGS 통합 시각화 결과 보고서 개선

이 문서는 사용자의 요청에 따라 단일 환자 데이터 케이스로부터 도출되는 모든 2D 및 3D 데이터(입력 원본 비디오부터 최종 재구성 메쉬 및 정량 지표까지)를 하나의 이미지로 통합하여 보고서 품질을 향상시킨 과정과 결과를 기록합니다.

---

## 1. 주요 변경 및 개선 내용

### 1.1 통합 시각화 스크립트 수정 (`optimize_and_visualize_3dgs_complete_grid.py`)
기존 2행 6열(2x6) 그리드에서 정보 제공이 부재하거나 중복되는 텍스트 상자 영역을 제거하고, 사용자가 요청한 **모든 2D/3D 핵심 단계별 산출물**을 완벽하게 매핑하도록 플로팅 로직을 전면 개편했습니다:
- **좌측 원본 이미지 배치**: 1행 1열과 2행 1열에 각각 전처리된 **원본 Angiogram 영상 (Raw View 1 & 2)**을 배치하여 분석의 시작점을 명확히 했습니다.
- **초기 및 최적화 3D 중심선 대비**: 1행 5열에 최적화 전 **3DGS Seed Points (초기 3D 삼각측량 결과)**를, 2행 5열에 최적화 후 **3D Centerline Trajectory Reconstructed**를 각각 배치하여 최적화 전후의 혈관 경로 정합 개선을 직관적으로 증명했습니다.
- **3D 뷰 앵글 통일**: `3DGS Seed Points`, `Optimized 3D Centerline Trajectory`, `3D Solid Vessel Mesh` 등 3가지 3차원 플롯에 대해 동일한 뷰잉 각도(`elev=30, azim=-60`)를 유지하도록 제약하여 기하학적 형상을 1:1로 직접 비교할 수 있도록 제어했습니다.
- **네이밍 통일**: 서브플롯 타이틀을 사용자가 지정한 명칭(`3DGS Mask Loss`, `Optimized 3DGS`, `3D Solid Vessel Mesh`, `Vessel Diameter Profile` 등)으로 정밀 수정하였습니다.

### 1.2 VM GPU 가속 기반 플롯 재생성 및 로컬 다운로드
- 수정된 스크립트를 VM(`34.64.94.136`)에 배포하고, GPU 가속 연산을 통해 Validation 셋 Top 10 Best 환자 데이터에 대해 12패널 통합 시각화 이미지(`best_01_complete_report.png` ~ `best_10_complete_report.png`)를 고해상도(300 DPI)로 완벽히 갱신하였습니다.
- 생성된 10개의 결과 이미지를 로컬 머신의 아티팩트 디렉토리(`/Users/home/.gemini/antigravity/brain/af8691ec-7f2d-4229-a6a4-e8f4635b5bd9/best_3dgs/`)로 성공적으로 동기화하였습니다.

### 1.3 메인 단계 연구 보고서 업데이트 (`main_phase_report.md`)
- Section 5.2의 이미지 참조 대상을 기존 5패널 3DGS 복원 이미지(`best_*_3dgs_reconstruction.png`)에서 새로운 12패널 통합 시각화 결과물(`best_*_complete_report.png`)로 변경했습니다.
- 12패널(2x6 Grid Layout)의 구성 요소 및 기하학적 의미를 항목별로 기술하여 독자가 보고서상의 각 서브플롯 역할을 직관적으로 이해할 수 있도록 명세하였습니다.

---

## 2. 통합 시각화 그리드 구성 (2행 6열)

각 환자 케이스별 12개 서브플롯은 다음과 같은 논리적인 흐름으로 구성됩니다:

| 열 | 1행 (View 1 관련 데이터 흐름) | 2행 (View 2 관련 데이터 흐름 및 정량 결과) |
| :---: | :--- | :--- |
| **1열** | **Raw View 1 Angiogram** (원본 입력) | **Raw View 2 Angiogram** (원본 입력) |
| **2열** | **View 1 Centerlines** (2D 및 3D 투영 중심선 오버레이) | **View 2 Centerlines** (2D 및 3D 투영 중심선 오버레이) |
| **3열** | **3DGS Mask Loss (View 1)** (최적화 전 렌더링-타겟 오차) | **3DGS Mask Loss (View 2)** (최적화 전 렌더링-타겟 오차) |
| **4열** | **Optimized 3DGS View 1** (최적화 완료 후 2D 마스크 정합) | **Optimized 3DGS View 2** (최적화 완료 후 2D 마스크 정합) |
| **5열** | **3DGS Seed Points** (초기 삼각측량 3D 뼈대) | **3D Centerline Trajectory** (최적화 완료 3D 중심선) |
| **6열** | **3D Solid Vessel Mesh** (Marching Cubes 3D 메쉬) | **Vessel Diameter Profile** (주요 분지 직경 프로파일) |

---

## 3. 결과 확인 및 검증

로컬 아티팩트 저장소에 저장된 이미지들의 파일 크기 및 생성 상태가 올바르게 검증되었습니다:
- **저장 경로**: `/Users/home/.gemini/antigravity/brain/af8691ec-7f2d-4229-a6a4-e8f4635b5bd9/best_3dgs/`
- **파일 규격**: 각 파일당 약 4.0 ~ 4.7 MB 크기의 고해상도(300 DPI, 42" x 14" 인치 캔버스) 이미지로 정상 갱신되었습니다.
- **시각적 품질**: 좌측에서 원본 영상 분석으로 시작해 우측의 3D 메쉬 및 직경 정량화 결과물로 이어지는 직관적인 데이터 진단 파이프라인이 하나의 통합 보고서 레이아웃 내에서 깔끔하게 구현되었습니다.

---

## 4. 대시보드 UI/UX 및 요소 렌더링 에러 해결

사용자 피드백에 따라 웹 대시보드(`index.html`) 상의 주요 렌더링 결함과 레이아웃 제한 사항들을 파악하고 즉시 수정하였습니다:

1. **LaTeX 수식 파싱 충돌 차단**:
   - 기존에는 `marked.js`가 수식 기호 내부의 언더바(`_`)나 더블 백슬래시(`\\`)를 HTML 이탤릭(`em`) 및 단일 백슬래시로 오인해 KaTeX가 깨지는 현상이 발생했습니다.
   - 마크다운 파싱 전 수식 영역을 식별하여 고유 ID가 부여된 `<div>` 및 `<span>` 태그로 먼저 보호한 후, 파싱이 완료된 DOM 상에 `katex.render` API로 직접 주입하는 구조로 개선하여 복잡한 수식이 완벽히 복구되었습니다.

2. **Mermaid v10 및 자바스크립트 예외 제어**:
   - Mermaid v10에서 deprecated 된 `mermaid.init()` 호출 문제를 `mermaid.run({ querySelector: '.mermaid' })`로 해결했습니다.
   - 타사 라이브러리(Mermaid, KaTeX 등) 파싱 영역에 각각 독립된 `try...catch`를 적용하여 수식이나 다이어그램 에러가 발생해도 목차(TOC) 생성 등의 핵심 스크립트가 멈추지 않도록 설계적 안정성을 높였습니다.

3. **GFM Alert 스타일 마크다운 보존**:
   - `[!NOTE]`와 같은 경고 블록 내부의 글씨 강조(`**`)나 인라인 수식이 마크다운 파싱을 거쳐 정상 렌더링되도록, 렌더링 완료 후 DOM 상에서 blockquote 패턴을 찾아 스타일을 입히는 후처리 구조로 개선하였습니다.

4. **도트 내비게이션 및 반응형 웹 고도화**:
   - 카루셀 내부 이미지들만 정확히 센싱하여 도트 인디케이터 개수를 일치시켰습니다.
   - CSS 미디어 쿼리를 전면 추가하여 모바일 환경(768px 이하)과 태블릿 화면 등 스크린 해상도가 낮아질 경우 목차가 자동으로 감춰지고 사이드바가 상단 탭 메뉴로 유연하게 변화하는 반응형 UX를 구축했습니다.

